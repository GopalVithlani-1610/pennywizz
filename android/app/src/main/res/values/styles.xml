<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <item name="android:textColor">#000000</item>
        <item name="android:navigationBarColor">#FFFFFF</item><!--Background color for system navigationbar-->
        <item name="android:windowSplashScreenBackground">#FFFFFF</item>
        <item name="android:colorBackgroundFloating">#FFFFFF</item>   <!-- Background Color for popup's -->
        <item name="android:textColorSecondary">#000000</item>
        <item name="android:textColorPrimary">#000000</item>
        <item name="android:textColorAlertDialogListItem">#000000</item>
        <item name="android:cacheColorHint">#3976fb</item>  <!-- Fading color code -->
        <item name="android:windowBackground">#262626</item>
    </style>

</resources>
